/**
 * Le type de données [Number] prends en charge 
 * tout type de données numérique(integer, double, float, etc)
 */
let nbreEntier = 12;
let nbreDecimal = 13.7;

console.log(nbreEntier);

console.log(typeof(nbreDecimal)); //typeof returne le type de données

/**
 * Les opérateurs mathématiques [*, -, +, **, %]
 */
let likes = 2 * (2**2) + (4/3) % 2
console.log(likes);
